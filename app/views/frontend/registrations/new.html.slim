.form-container
  = simple_form_for @member, as: :member, url: create_member_path, html: { class: 'form-horizontal' } do |f|
    .login-container
      .home-title 
          div
              = icon('fas', 'address-card')
              h1 Cadastro
          p Realize aqui o seu cadastro!
    
      .input-container
        .input-box
          div
            .text-i = icon('fas', 'envelope')
            = f.input_field :email, placeholder: 'Digite seu E-mail', required: true, autofocus: true, class: 'login-input'
          
          - if @member.errors.has_key?(:email)
            span.text-danger = @member.errors[:email].first
          span.text-danger id="emailMessage"

        .input-box
          div
            .text-i = icon('fas', 'id-card')
            = f.input_field :name, placeholder: 'Digite seu Nome', required: true, class: 'login-input'
          - if @member.errors.has_key?(:name)
            span.text-danger = @member.errors[:name].first

        .input-box
          div
            .text-i = icon('fas', 'mobile')
            = f.input_field :phone, label: 'Telefone', placeholder: 'Digite seu Telefone', autofocus: true, required: true, input_html: { id: 'phone_field' }, class: 'login-input'
          - if @member.errors.has_key?(:phone)
            span.text-danger = @member.errors[:phone].first
          span#phoneMessage

        .input-box
          div
            .text-i = icon('fas', 'id-card')
            = f.input_field :cpf, label: 'CPF', placeholder: 'Digite seu CPF', required: true, autofocus: true, class: 'login-input'
          - if @member.errors.has_key?(:cpf)
            span.text-danger = @member.errors[:cpf].first
          span id="cpfMessage"


        .button-container
          .recaptcha = recaptcha_tags
          = f.button :submit, "Enviar", class: 'button-register', id: 'button-submit', disabled: true
          = link_to "Voltar", "/", class: 'button-register'